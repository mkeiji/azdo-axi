## 1. Workflow

- [x] 1.1 Add a GitHub Actions workflow triggered by pull requests and pushes to `master`.
- [x] 1.2 Configure non-interactive checkout, Node.js setup, lockfile-based `npm ci`, and separate `npm test` and `npm run check` steps without Azure DevOps secrets.

## 2. Validation

- [x] 2.1 Validate the OpenSpec change and run the workflow-equivalent dependency installation, test, and TypeScript checks locally.
